#include <vector>

template<typename T>
class vektor 
{
	public:
					vektor();
					~vektor();
};

template<typename T>
vektor<T>::vektor()
{
	return;
}

template<typename T>
vektor<T>::~vektor()
{
	return;
}